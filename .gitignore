node_modules
.env
.env.*
!.env.example
dist

/src/generated

# Lock Files — npm's package-lock.json IS committed for reproducible installs
pnpm-lock.yaml
yarn.lock
pnpm-workspace.yaml

.vscode

# Local-only Postgres cluster (see docs/decisions.md) — a separate, self-owned
# dev DB on port 5433 so local dev/tests don't depend on the machine's
# system-wide Postgres service credentials.
.devdb

# Claude Code tooling (skills, worktrees) — local only, never published
.claude

# Personal — kept local, never pushed
docs/MY-INTENT.md
